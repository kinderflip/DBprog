# IT8415 Blog — Block server-side script execution in images/
# Minimal directive that works on all Apache 2.4+ configs.

<FilesMatch "\.(php|phtml|phar|pl|py|cgi|sh)$">
    Require all denied
</FilesMatch>
